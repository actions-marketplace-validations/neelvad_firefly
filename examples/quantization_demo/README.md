# Quantization demo (placeholder)

The killer demo for Firefly.

**Plan:** Take SmolLM-135M, deliberately break one layer's weights, and show
Firefly pinpointing the exact layer the divergence originated at — while a
clean re-run produces zero divergence.

To be filled in once the capture/check pipeline is real.
